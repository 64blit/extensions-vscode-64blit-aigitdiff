import * as vscode from 'vscode';

export function activate(_context: vscode.ExtensionContext) {
  // Extension scaffold. Commands and providers will be registered here.
}

export function deactivate() {}
